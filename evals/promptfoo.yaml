description: "Feedback classification agent evaluation suite"

providers:
  - id: "file://providers.js"

prompts:
  - "{{feedback}}"

tests: file://test-cases.json
defaultTest:
  assert:
    - type: latency
      threshold: 40000 # milliseconds
